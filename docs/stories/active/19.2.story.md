# Story 19.2 — Worker polling on-chain v4

**Epic:** Epic 19 — Monitor v4 Subaccount + Canal Discord
**Status:** Ready for Review
**Executor:** @dev
**Data:** 2026-03-23

## Goal

Worker que faz polling na Polygon a cada 5 minutos buscando eventos Transfer
de USDT e LOOP envolvendo o subaccount v4. Persiste em `v4_events`.

## Acceptance Criteria

- [x] Worker `v4_subaccount_worker()` com `while True` + `time.sleep(300)`
- [x] Busca eventos Transfer ERC-20 do subaccount (from OU to) em USDT e LOOP
- [x] Usa `get_logs` com filtro `topics[1]` e `topics[2]` (from/to indexados)
- [x] Persiste eventos novos em `v4_events` com `reported=0`
- [x] `tx_hash` UNIQUE previne duplicatas
- [x] Usa `RPC_FALLBACK` como fallback se `RPC_URL` falhar
- [x] Bloco inicial: último bloco salvo em `v4_reports.last_block` ou `latest - 1000`
- [x] Logs informativos a cada ciclo

## File List

| Arquivo | Ação |
|---------|------|
| `packages/monitor-engine/webdex_v4_monitor.py` | Criado |
