# Story 16.4 — QA Audit: Workers (subscription + notification + creatomate)

**Epic:** Epic 16 — QA Audit Monitor Engine
**Status:** ✅ Ready for Review
**Criado por:** @sm (Niobe)
**Data:** 2026-03-20
**Executor:** `@qa`
**Dependência:** 16.1 (conftest.py)

---

## 🎯 Goal

Criar testes para os workers críticos:
- `subscription_worker.py` (440L) — polling on-chain, ativa Pro automaticamente
- `notification_engine.py` (341L) — notificações automáticas Telegram + Discord
- `creatomate_worker.py` (408L) — geração de vídeos ciclo 21h

---

## 📋 Acceptance Criteria

### AC1 — Testes subscription_worker.py (CRÍTICO — receita)
- [ ] `test_persist_subscription_idempotent` — mesmo evento 3x → 1 registro
- [ ] `test_wallet_lock_prevents_race` — 2 threads na mesma wallet → serializa
- [ ] `test_wallet_lock_timeout` — lock timeout não trava o worker
- [ ] `test_process_event_invalid_data` — evento malformado não quebra o loop
- [ ] `test_checkpoint_advances` — sub_last_block incrementa após ciclo
- [ ] `test_notify_when_chat_id_found` — Telegram notificado quando wallet vinculada

### AC2 — Testes notification_engine.py
- [ ] `test_milestone_detection` — milestone detectado quando threshold atingido
- [ ] `test_notification_not_duplicate` — mesma notificação não enviada 2x em 24h
- [ ] `test_notification_graceful_fail` — falha no envio não derruba o worker
- [ ] `test_discord_webhook_format` — payload Discord formatado corretamente

### AC3 — Testes creatomate_worker.py
- [ ] `test_gerar_video_ciclo_params` — parâmetros corretos enviados para API
- [ ] `test_poll_timeout_behavior` — timeout de 180s não trava o bot
- [ ] `test_graceful_on_api_error` — erro na API Creatomate → log + continua

### AC4 — Coverage
- [ ] Coverage >= 50% em subscription_worker.py
- [ ] Coverage >= 40% em notification_engine.py
- [ ] Coverage >= 35% em creatomate_worker.py

---

## 📁 File List

| Arquivo | Ação |
|---------|------|
| `packages/monitor-engine/tests/test_subscription_worker.py` | CRIAR |
| `packages/monitor-engine/tests/test_notification_engine.py` | CRIAR |
| `packages/monitor-engine/tests/test_creatomate_worker.py` | CRIAR |

---

## Dev Agent Record

- [x] AC1 — test_subscription_worker.py (idempotência + race condition) — 19 testes passando
- [x] AC2 — test_notification_engine.py (dedup + graceful fail) — 20 testes passando
- [x] AC3 — test_creatomate_worker.py (API timeout + error handling) — 13 testes passando
- [x] AC4 — Coverage >= targets — workers críticos cobertos
