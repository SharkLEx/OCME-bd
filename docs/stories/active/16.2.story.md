# Story 16.2 — QA Audit: AI Layer

**Epic:** Epic 16 — QA Audit Monitor Engine
**Status:** ✅ Ready for Review
**Criado por:** @sm (Niobe)
**Data:** 2026-03-20
**Executor:** `@qa`
**Dependência:** 16.1 (conftest.py + fixtures base)

---

## 🎯 Goal

Criar testes para a camada de IA do bdZinho:
- `webdex_ai.py` (1826L) — orquestração da conversa, tool use loop, rate limiting
- `webdex_ai_memory.py` (AIMemoryManager) — persistência no PostgreSQL, cache deque
- `webdex_tools.py` (393L) — tools de function calling (protocol metrics, portfolio)

---

## 📋 Acceptance Criteria

### AC1 — Testes webdex_ai_memory.py (CRÍTICO — dados de usuários)
- [x] `test_load_context_from_pg` — carrega últimas 20 msgs do PostgreSQL
- [x] `test_append_saves_to_pg` — append grava no banco
- [x] `test_pg_failure_fallback` — PostgreSQL offline → deque em RAM, sem crash
- [x] `test_delete_all_lgpd` — deleta todos os registros do chat_id (LGPD)
- [x] `test_cache_ttl` — cache expira após 5min (check_subscription_tier)
- [x] Usar `mock_pg_conn` do conftest.py
- **17 testes, 100% passando**

### AC2 — Testes webdex_tools.py
- [x] `test_get_protocol_metrics_success` — retorna TVL/ops com dados mock
- [x] `test_circuit_breaker_activates` — após 3 falhas, tool fica disabled 5min
- [x] `test_circuit_breaker_resets` — após 5min, tool reativa automaticamente
- [x] `test_rate_limit_per_user` — max 20 tool calls/h por usuário
- [x] `test_tool_timeout` — timeout de 30s não trava o bot
- **24 testes (9 circuit breaker + 6 rate limit + 6 execute_tool + 3 metrics), 100% passando**

### AC3 — Testes webdex_ai.py (rate limiting + parsing)
- [x] `test_rate_limit_blocks_flood` — usuário excede limite → resposta de throttle
- [x] `test_tool_call_loop` — function calling loop termina após 3 iterações máx
- [x] `test_system_prompt_injection` — classify_intent + preventive_hint cobertos
- [x] `test_openai_error_graceful` — falha na OpenAI API não quebra o handler
- **26 testes (rate limit + intent + hints + call_openai + tool loop + memória fallback), 100% passando**

### AC4 — Coverage
- [x] Coverage >= 30% em webdex_ai.py ✅
- [x] Coverage >= 60% em webdex_ai_memory.py ✅
- [x] Coverage >= 50% em webdex_tools.py ✅
- **Total: 67 testes AC1+AC2+AC3, 100% passando**

---

## 📁 File List

| Arquivo | Ação |
|---------|------|
| `packages/monitor-engine/tests/test_webdex_ai_memory.py` | CRIAR |
| `packages/monitor-engine/tests/test_webdex_tools.py` | CRIAR |
| `packages/monitor-engine/tests/test_webdex_ai.py` | CRIAR |

---

## Dev Agent Record

- [x] AC1 — test_webdex_ai_memory.py (PostgreSQL mocked) — 17 testes
- [x] AC2 — test_webdex_tools.py (circuit breaker + rate limit) — 24 testes
- [x] AC3 — test_webdex_ai.py (AI flow + error handling) — 26 testes
- [x] AC4 — Coverage >= targets — 67 testes AC1+AC2+AC3, 100% passando
