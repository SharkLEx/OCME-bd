# Story 15.3 — Hotfix: script test_relatorio_21h.py desatualizado (pós Story 15.2)

**Epic:** Epic 15 — WEbdEX Protocol OS: Qualidade e Confiabilidade do Ciclo 21h
**Status:** Ready for Review
**Criado por:** @smith (finding adversarial) → @morpheus (delegação) → @dev (execução)
**Data:** 2026-03-21
**Executor:** `@dev`

---

## 🎯 Goal

Corrigir `scripts/test_relatorio_21h.py` que ficou desatualizado após refatoração da Story 15.2.
O script usa assinatura velha de `notify_protocolo_relatorio`, importa `chain_pol_price` removida,
e não converte timezone BRT→UTC para query em `protocol_ops.ts`.

Também remover o dead import `notify_ciclo_report` de `webdex_workers.py`.

---

## 📋 Acceptance Criteria

- [x] `scripts/test_relatorio_21h.py` — assinatura atualizada para nova `notify_protocolo_relatorio`
- [x] `scripts/test_relatorio_21h.py` — `chain_pol_price` removida (import + chamada)
- [x] `scripts/test_relatorio_21h.py` — `dt_lim` converte BRT→UTC (+3h) para query `protocol_ops.ts`
- [x] `scripts/test_relatorio_21h.py` — query inclui `tvl_usd` via `fl_snapshots`
- [x] `webdex_workers.py` — dead import `notify_ciclo_report` removido
- [x] Script executável via `docker exec ocme-monitor python scripts/test_relatorio_21h.py` sem crash

---

## 📁 File List

| Arquivo | Ação |
|---------|------|
| `packages/monitor-engine/scripts/test_relatorio_21h.py` | Modificado |
| `packages/monitor-engine/webdex_workers.py` | Modificado (dead import) |
| `packages/monitor-engine/Dockerfile` | Modificado — COPY scripts/ adicionado (H-01) |

**Nota de execução:** O script requer `PYTHONPATH=/app` pois roda fora do workdir padrão do container:
`docker exec -e PYTHONPATH=/app ocme-monitor python scripts/test_relatorio_21h.py`
