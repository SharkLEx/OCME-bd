# Story 19.1 — Schema v4_events + v4_reports

**Epic:** Epic 19 — Monitor v4 Subaccount + Canal Discord
**Status:** Ready for Review
**Executor:** @dev
**Data:** 2026-03-23

## Goal

Criar as tabelas SQLite `v4_events` e `v4_reports` no banco existente `webdex_v5_final.db`,
seguindo o padrão de `webdex_db.py`.

## Acceptance Criteria

- [x] Tabela `v4_events` criada com colunas: id, ts, tx_hash (UNIQUE), event_type, token_sym, amount_raw, amount_usd, from_addr, to_addr, block_num, reported
- [x] Tabela `v4_reports` criada com colunas: id, ts, period_start, period_end, total_in_usd, total_out_usd, tx_count, discord_sent
- [x] Índices em `v4_events(ts)`, `v4_events(reported)`, `v4_events(tx_hash)`
- [x] Schema criado via `_ensure_v4_schema()` chamado no startup do banco

## File List

| Arquivo | Ação |
|---------|------|
| `packages/monitor-engine/webdex_v4_monitor.py` | Criado (novo) |
